# 1.12. The "How-To" of branching.

<!-- pause -->
<!-- new_line -->

Let's hop back into the command line:

<!-- new_line -->

```file +render
path: ../visuals/1_12_git_branching.typ
language: typst
```

<!-- pause -->
<!-- new_line -->

So, what is the `observation`?

<!-- new_line -->
<!-- incremental_lists: true -->

- `git switch` by default expects the target branch to `already exist`.
- You can create a new branch pointing to the current `HEAD` commit using the
  `-c` flag.
- You can optionally add a `commit id` you want to branch/diverge from.
