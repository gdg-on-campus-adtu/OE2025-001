# 1.15. Remotes?

<!-- pause -->
<!-- new_line -->

So far, we've only been working with Git locally. That's no fun. The `D` in
`DVCS` stands for distributed after all.

<!-- pause -->
<!-- new_line -->

`->` So, to sum it up simple, `remotes` are a hosted version of your repository.

<!-- pause -->

`->` Remotes are the very thing that enables `collaboration` among `developers`
over the world.

<!-- pause -->
<!-- new_line -->

So, lets dive right into the command line:

<!-- new_line -->

```file +render
path: ../visuals/1_15_git_remotes.typ
language: typst
```

<!-- pause -->
<!-- new_line -->

`->` The astute ones among you are already wondering about `force pushes`.
